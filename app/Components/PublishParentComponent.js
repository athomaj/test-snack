import React from 'react';
import { View } from 'react-native';

export default PublishParentComponent = ({ navigation }) => {

    return (
        <View style={{ height: '100%', width: '100%', backgroundColor: 'white' }}>
        </View>
    );
}